# AGENTS.md

このリポジトリはチケノートのproduction配信用データのみを扱う。

- 収集・巡回・AI処理の実装を持ち込まない。
- 根拠のない値を推測で補完しない。
- 既存データを整理目的だけで削除・再構成しない。
- production反映は対象差分を確認し、ユーザーの明示承認がある場合だけ行う。
- 変更は依頼範囲に限定する。

データ変更時は `.agents/skills/project-change` を使用する。read-only確認では不要。
